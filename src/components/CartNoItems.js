import React from 'react';

import { Typography } from '@material-ui/core';

const CartNoItems = () => (
  <Typography
    variant="h6"
    color="textSecondary"
    align="center"
    style={{ marginTop: 100 }}
  >
    No items
  </Typography>
);

export default CartNoItems;
